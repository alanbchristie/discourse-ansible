---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: discourse-sidekiq
  namespace: {{ d_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: '{{ d_sidekiq_vol_storageclass }}'
  resources:
    requests:
      storage: '{{ d_sidekiq_vol_size_g }}Gi'
