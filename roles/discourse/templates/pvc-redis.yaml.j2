---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: redis
  namespace: {{ d_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
{% if d_redis_vol_storageclass != ' ' %}
  storageClassName: '{{ d_redis_vol_storageclass }}'
{% endif %}
  resources:
    requests:
      storage: '{{ d_redis_vol_size_g }}Gi'
