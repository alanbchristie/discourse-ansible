---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres
  namespace: {{ d_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: '{{ d_postgres_vol_storageclass }}'
  resources:
    requests:
      storage: '{{ d_postgres_vol_size_g }}Gi'
